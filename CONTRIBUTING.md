# 为 Localis 做贡献

感谢你帮助改善 Localis。提交前请先确认改动没有破坏项目最重要的边界：**管理和计算发生在电脑，头显只访问局域网页面。**

## 开发流程

1. 从 `main` 创建短分支，并让一次提交只解决一个明确问题。
2. 不要提交真实媒体、网盘凭据、Cookie、token、证书、私钥、绝对本机路径或转码缓存。
3. 对行为变化补充 Vitest 测试；播放链路变化还应使用真实 FFmpeg 输出验证。
4. 运行以下质量门槛：

```powershell
npm run lint
npm run typecheck
npm test
npm run build
```

5. 在 Pull Request 中说明设备、Windows 版本、浏览器、网络结构、输入媒体编码和实际观察结果。模拟协议测试必须标记为模拟，不能写成真实网盘账号或真实头显已测。

## 设计约束

- UI 使用原生 HTML/CSS 图标或仓库视觉系统，不使用 emoji 充当播放器控件。
- 保持深石墨、暖白和荧光绿的克制配色；优先清晰层级、留白和空间感。
- LAN 页面不暴露电脑路径、配对码、云盘管理、文件夹选择或上游凭据。
- 大文件必须支持 Range 或按需 HLS，不要让长电影为了跳转而从片头顺序计算。

当前源码许可证保留所有权利。提交代码不自动改变仓库许可；外部贡献需由维护者单独确认授权条件。
