# 安全策略

## 报告漏洞

请不要在公开 Issue 中披露可利用漏洞、真实局域网地址、网盘凭据、Cookie、token、证书私钥或本机媒体路径。请通过 GitHub 仓库的私密漏洞报告功能联系维护者，并提供：受影响版本、复现条件、影响范围和最小复现步骤。

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| 最新 Release | 支持 |
| 更早版本 | 尽力支持 |

## 设计边界

Localis 面向可信家庭或工作室局域网，不应直接把局域网监听端口映射到公网。WebXR HTTPS 应使用公共可信证书。头显页面只能执行配对与播放，不能访问主机管理接口。

`Android_beta` 允许用户显式配置一个精确 HTTPS 外部来源，以便 Cloudflare Tunnel 等反向代理提供 WebXR 安全上下文。这不等于应将媒体服务无保护地公开到互联网：六位配对码只是设备配对机制，不是公网账户系统。面向公网时必须先用 Cloudflare Access 或等效的反向代理身份验证限制可访问用户，不要覆盖公网 `Host`，并保持入站防火墙不直接开放 Android 端口。服务器不信任 `Forwarded` 或 `X-Forwarded-*` 来决定安全来源。
